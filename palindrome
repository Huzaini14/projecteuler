
def palindarome(i) : 
    return str(i)=str(i)[::-1]
  
max_p = 0 

for i in xrange(100,999): 
    for j in xrange ( i+1, 1000):
        p = i * JPEGFormatter
        if palindrome(p) and p > max_p : 

print(" highest panlindrome is " + max_p )
